# =============================================================
# 사내 폐쇄망 LightRAG 설정 파일
# 이 파일을 .env.onprem 으로 복사한 뒤 값을 채우세요.
# =============================================================

# --- LLM API ---
ONPREM_LLM_BASE_URL=http://apigw-stg.shyum.net:8000/gpt-oss/1/gpt-oss-120b/v1
ONPREM_LLM_MODEL=openai/gpt-oss-120b
ONPREM_LLM_CREDENTIAL_KEY=여기에_DS_API_HUB_credential_key
ONPREM_SEND_SYSTEM_NAME=여기에_DS_API_HUB_등록_Send-System-Name
ONPREM_USER_ID=여기에_KNOX_ID
# 폐쇄망에서 tokenizer 원격 다운로드를 피하기 위해 고정 모델명을 사용
# (인터넷 가능한 환경에서 동작 확인한 모델명으로 변경 권장)
ONPREM_TIKTOKEN_MODEL=text-embedding-3-small
# true면 tiktoken 대신 오프라인 문자 기반 tokenizer를 사용(권장)
ONPREM_USE_OFFLINE_CHAR_TOKENIZER=true
# tokenizer 선택: char | xlmr
ONPREM_TOKENIZER_TYPE=char
# ONPREM_TOKENIZER_TYPE=xlmr 일 때 로컬 tokenizer 경로
ONPREM_XLMR_TOKENIZER_PATH=./tokenizer

# --- 임베딩 (로컬 FlagEmbedding, Ollama NaN 문제 해결) ---
# HuggingFace에서 다운로드한 BAAI/bge-m3 모델의 로컬 경로
ONPREM_EMBED_MODEL_PATH=./models/bge-m3
# GPU 사용 여부 (false면 CPU — 느리지만 CUDA 없어도 동작)
ONPREM_EMBED_USE_GPU=true
# bge-m3 임베딩 차원
ONPREM_EMBED_DIM=1024
# 긴 문서에서 gleaning 토큰 초과 경고가 나면 아래 값을 더 줄여서 조정
ONPREM_CHUNK_TOKEN_SIZE=800
ONPREM_CHUNK_OVERLAP_TOKEN_SIZE=80
# 추출 단계 입력 토큰 상한 (모델 컨텍스트에 맞춰 조정)
ONPREM_MAX_EXTRACT_INPUT_TOKENS=16000

# --- Reranker API ---
ONPREM_RERANK_BASE_URL=http://apigw-stg.shyum.net:8000/reranker/1/v2/rerank
ONPREM_RERANK_MODEL=bge-reranker-v2-m3-ko
ONPREM_RERANK_CREDENTIAL_KEY=여기에_reranker_credential_key
ONPREM_RERANK_TOP_N=5
